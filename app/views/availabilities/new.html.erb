<% content_for :title,  t('.title') %>

<div class="wrapper">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-8 col-sm-offset-2">
          <% if flash[:exceed_car_limit] %>
            <div class="alert-warning"><%= flash[:exceed_car_limit] %></div>
          <% end %>
        <h2> <%= t('.title') %> </h2>
        <%= render 'availability_form' %>
      </div>
    </div>
  </div>
</div>
