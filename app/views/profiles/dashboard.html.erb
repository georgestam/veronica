<div class="white-wrapper padded">
  <div class="tabs">
    <a class="tab active" href="#">
      <h3>Overview</h3>
    </a>
    <a class="tab" href="#">
      <h3>Journeys</h3>
    </a>
    <a class="tab" href="#">
      <h3>Notifcations</h3>
    </a>
    <a class="tab" href="#">
      <h3>Account settings</h3>
    </a>
  </div>

  <div class="dashboard-wrapper">
    <div class="overview-header">
      <% if @user.photo? %>
        <%= cl_image_tag @user.photo.path, class: "avatar-large header-img" %>
      <% else %>
        <%= image_tag "http://placehold.it", class: "avatar-large header-img" %>
      <% end %>
      <div class="header-info">
        <h3><%= @user.full_name %></h3>
        <%= link_to "View public profile", profile_path(@user) %>
      </div>
      <div class="header-progress">
        <div class="progress">
          <div class="progress-bar progress-bar-success" role="progressbar" aria-valuenow="20"
          aria-valuemin="0" aria-valuemax="100" style="width:20%" id="account-progress">
            20% Complete
          </div>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-xs-12 col-sm-4 hidden-xs">
          <div class="overview-card">
            <h2>Verification</h2>
            <p>Email verification</p>
            <p>Payment verification</p>
            <p>Student ID verification</p>
            <p>Car verification</p>
            <h2>Journey stats</h2>
            <p>No. of journeys: </p>
            <p>Avg. journey time:</p>
            <p>Miles travelled: </p>
            <h2>Preferences</h2>
            <p>Music: </p>
            <p>Talking: </p>
            <p>Smoking: </p>
          </div>
        </div>
        <div class="col-xs-12 col-sm-8">
          <div class="overview-card car-card">
            <% if @user.cars.empty? %>
              <p>You haven't uploaded your car details yet!</p>
              <%= link_to "Add your car detials", new_car_path, class: "btn btn-primary" %>
            <% else %>
              <h3>Car details</h3>
              <% @user.cars.each do |car| %>
                <p>Make: <%= car.make %></p>
                <p>Model: <%= car.name %></p>
                <p>Colour: <%= car.colour %></p>
                <p>Licence: <%= car.vrn %></p>
              <% end %>
            <% end %>
          </div>
          <div class="overview-card">
            <% if @journeys %>
              <p>You haven't been reviewed yet!</p>
            <% else %>
              <h3>Reviews</h3>
              <p>Rating: <%= @avg_rating %> </p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
