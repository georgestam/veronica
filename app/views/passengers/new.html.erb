<div class='lift-bacground'>

<% content_for :title, "Add review" %>

<div class="wrapper">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-8 col-sm-offset-2">
          <% if flash[:exceed_car_limit] %>
            <div class="alert-warning"><%= flash[:exceed_car_limit] %></div>
          <% end %>
        <%= render 'review_form' %>
      </div>
    </div>
  </div>
</div>

</div>
